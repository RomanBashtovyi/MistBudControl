# Наступні кроки для завершення сайту

## ✅ Що вже готово:

- [x] Проект ініціалізовано (Next.js 16 + TypeScript + Tailwind)
- [x] Header та Footer з навігацією
- [x] Головна сторінка з усіма секціями
- [x] Сторінка проектів
- [x] Система для додавання проектів через JSON
- [x] Базова адаптивність (mobile menu)
- [x] TypeScript типізація
- [x] Константи для легкого редагування контенту

## 📝 Що треба зробити для продакшну:

### 1. Замінити контент на реальний

#### Контактні дані директора

Файл: `constants/site.ts`

```typescript
export const DIRECTOR_CONTACT: ContactInfo = {
  name: "Ім'я Прізвище", // ← замінити
  position: 'Директор',
  phone: '+38 (0XX) XXX-XX-XX', // ← замінити
  email: 'real@mistbudcontrol.ua', // ← замінити
  photo: '/team/director.jpg', // ← додати фото
}
```

#### Фото директора

- Додати фото: `public/team/director.jpg`
- Рекомендований розмір: 200x200px або більше (квадратне)

### 2. Додати реальні проекти

**Кожен проект:**

1. Створити папку: `data/projects/[назва-проекту]/`
2. Додати `manifest.json` (див. `data/projects/README.md`)
3. Додати фото: `public/projects/[назва-проекту]/`
   - `thumbnail.jpg` - головне (800x600px)
   - `01.jpg`, `02.jpg`, ... - додаткові (1200x800px)

**Рекомендації:**

- Використовуйте WebP для кращої оптимізації
- Стискайте зображення перед завантаженням
- Featured проекти (`featured: true`) з'являться на головній

### 3. Логотипи партнерів

**Зараз:** заглушки "Logo"
**Треба:**

1. Створити папку: `public/partners/`
2. Додати логотипи:
   - `kontakt.png`
   - `agroresurs.png`
   - `prombudcentr.png`
   - `scanmix.png`

**Формат:** PNG з прозорим фоном, ~400x200px

### 4. Оптимізація зображень

```bash
# Встановити sharp (вже є)
npm install sharp

# Next.js автоматично оптимізує зображення через Image component
```

### 5. SEO оптимізація

#### Мета-теги (вже є базові в `app/layout.tsx`)

Можна додати:

- Open Graph теги
- Twitter Card теги
- Favicon (заміна favicon.ico)

#### Створити sitemap.xml

```bash
# Next.js може генерувати автоматично
# Створити app/sitemap.ts
```

### 6. Аналітика (опціонально)

#### Google Analytics

```bash
npm install @next/third-parties
```

Додати в `app/layout.tsx`:

```typescript
import { GoogleAnalytics } from '@next/third-parties/google'

// В компоненті:
;<GoogleAnalytics gaId="G-XXXXXXXXXX" />
```

### 7. Форма зв'язку (опціонально)

Якщо потрібна контактна форма:

```bash
npx shadcn@latest add form input textarea
```

Створити компонент ContactForm з інтеграцією email (SendGrid, Resend, або проста API route)

### 8. Перед деплоєм

#### Перевірити все локально:

```bash
# Розробка
npm run dev

# Білд
npm run build

# Перевірити production білд
npm start
```

#### Оновити .gitignore (вже є)

Переконайтеся що не комітяться:

- node_modules
- .next
- .env (якщо використовуєте)

### 9. Деплой

#### Vercel (рекомендовано для Next.js):

```bash
npm install -g vercel
vercel
```

#### Або інші платформи:

- Netlify
- Railway
- DigitalOcean App Platform

### 10. Домен та SSL

1. Купити домен (наприклад: mistbudcontrol.ua)
2. Прив'язати до Vercel/хостингу
3. SSL сертифікат (автоматично на Vercel)

## 🎨 Кастомізація дизайну (якщо потрібно)

### Змінити кольори

Файл: `app/globals.css`

```css
:root {
  --primary: oklch(0.205 0 0); /* змінити колір */
  /* інші змінні... */
}
```

### Додати логотип замість зображення

Можна створити SVG лого замість JPG для кращої якості

### Змінити шрифти

Файл: `app/layout.tsx`

```typescript
// Замість Geist можна використати інші Google Fonts
import { Inter, Roboto } from 'next/font/google'
```

## 📊 Моніторинг після запуску

1. **Google Search Console** - індексація сайту
2. **PageSpeed Insights** - перевірка швидкості
3. **Lighthouse** - загальна якість

## 🔧 Додаткові можливості

### Якщо потрібно додати:

- **Блог** - створити `app/blog/`
- **Галерея** - lightbox для зображень проектів
- **Відгуки клієнтів** - нова секція
- **Калькулятор** - розрахунок вартості проекту
- **Багатомовність** - next-intl для UA/RU/EN

## 📞 Підтримка

Всі файли структуровані та прокоментовані.
Дивіться `README.md` для загальної інформації про архітектуру.

---

**Готово до запуску!** Просто додайте реальний контент і деплойте 🚀
